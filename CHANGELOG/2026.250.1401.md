# 2026.250.1401 — docs(layer): document service overrides block -> systemd drop-in + packaged units

## Summary

Documents the `service:` entry's `overrides:` block for `use_packaged:` units in the layer skill (`/charly-image:layer`), accurate to the merged code (sdk #238):

- A `use_packaged:` entry may declare an `overrides:` block (`exec` / `env` / `after`) — the override materializes as a systemd DROP-IN on the packaged unit, never a regenerated unit file. On deploy, sdk/deploykit's packaged-service compile (`CompileServiceSteps`) renders the drop-in (`OverridesText` / `OverridesPath` via the init-provider render seam), and the executor writes it + daemon-reloads BEFORE `enable --now` — the service starts with the overridden `ExecStart` / env / ordering while the SHIPPED unit stays untouched (worked example: an `exec` override rebinding `charly-mcp.service` from its packaged loopback default to `0.0.0.0:18765`, the exact case the check-cachyos-mcp-vm bed proved).
- Adds the packaged-units subsection: the generated charly packages (deb/rpm/archlinux, built by `charly generate-packages` / sdk/packagekit) ship non-autostarting system + user `charly-mcp` systemd units plus a system-wide `/etc/charly/charly.yml` — installed, NEVER enabled (apk/ipk carry no systemd).

## How tested

- `grep`/read-verification of the merged source facts: sdk PR #238 (merged, `deploykit/compile_service_steps.go` — `OverridesText`/`OverridesPath`; executor `walkServicePackaged`: write drop-in → daemon-reload → `enable --now`) and `charly/packaging/charly.yml` (non-autostarting system+user `charly-mcp` units, `/etc/charly/charly.yml`, deb/rpm/archlinux only).
- `python3 yaml.safe_load` on the edited `charly.yml` — parses; `layer-skill.skill.content` remains a single scalar string; all 10 factual assertions on the new text pass.
- YAML LSP diagnostics: no diagnostics.
- `git diff` — 20 insertions in `charly.yml`, no deletions; tree clean after commit; branch pushed.

\`\`\`console
$ python3 /tmp/check_layer_docs.py
OK: yaml parses, content scalar, all 10 facts present
\`\`\`

## Rulebook compliance

- R0: task-driven doc edit; no skill load required beyond the repo rulebook (docs-only change class).
- R6: branch `feat/service-overrides-dropin-docs` from `origin/main`, PR-only, no force-push, no direct-to-main push.
- R7: docs-only change — no gate command applicable; YAML parse + structure assertions pasted above.
- R10: verification from the committed tree state (post-commit `git status` clean, diff re-read from the commit).

## Change classification

- **Change class:** docs
- **Verification gate:** YAML parse + content-scalar assertions (docs-only; no build/test gate applicable)
- **Attribution tier:** documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*